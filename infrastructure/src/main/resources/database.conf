database {
    host = "localhost"
    host = ${?CONF_DB_HOST}
    port = 5432
    port = ${?CONF_DB_PORT}
    username = "gideok"
    username = ${?CONF_DB_USERNAME}
    password = "rlejr0525!"
    password = ${?CONF_DB_PASSWORD}
    database = "postgres"
    database = ${?CONF_DB_DATABASE}
    schema = "carpen"
    schema = ${?CONF_DB_SCHEMA}
}